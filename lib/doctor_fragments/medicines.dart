import 'package:flutter/material.dart';

class Medicines extends StatelessWidget {
  const Medicines({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Mdeicine Screen"),
      ),
    );
  }
}
